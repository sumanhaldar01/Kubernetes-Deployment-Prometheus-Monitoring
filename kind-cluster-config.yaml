kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: test-cluster
nodes:
- role: control-plane
- role: worker
- role: worker

# command to execute "kind create cluster --config kind-cluster-config.yaml"
